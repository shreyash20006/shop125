{%- liquid
  assign target = product.selected_or_first_available_variant
  assign compare_at_price = target.compare_at_price
  assign price = target.price
-%}

<div class="price{% if compare_at_price > price %} price--on-sale{% endif %}">
  {%- if product.price_varies -%}
    <span class="price__from">{{ 'products.from_price' | t: price: price | money }}</span>
  {%- else -%}
    <span class="price__regular">{{ price | money }}</span>
    {%- if compare_at_price > price -%}
      <span class="price__compare"><s>{{ compare_at_price | money }}</s></span>
    {%- endif -%}
  {%- endif -%}
</div>
